# Лабораторная работа №13 - Мультиагентные системы: разработка распределённых интеллектуальных агентов 
**Студент:** Фризен Марк Владимирович
**Группа:** 221331
**Вариант:** 15
# Prompt Log
**Инструмент:** deepseek v4-flash
## Задание 1:  Разработка полной системы из 3–5 агентов на Go.
### Промпт 1
**Промпт:** "Создай структуру Go-агента для сегментации аудитории (маркетинг). Агент должен подписываться на NATS-очередь 'tasks.process', читать задачу типа 'segment', выполнять простую логику (деление клиентов на группы по возрасту/региону) и публиковать результат в 'tasks.completed'. Используй библиотеку nats.go. Добавь JSON-теги для структур Task и Result."
**Результат:** Создана структура Go-агента:
1 cmd/agents/segment/
     2 ├── go.mod          — зависимости: nats.go v1.52.0
     3 ├── go.sum
     4 ├── main.go         — подключение к NATS, подписка на tasks.process, graceful shutdown
     5 ├── segmenter.go    — логика: segmentByAge(), segmentByRegion(), segment()
     6 └── types.go        — struct Client, Task, Segment, Result (все с JSON-тегами)

### Промпт 2
**Промпт:** "Добавь в агента логирование с уровнями INFO и ERROR (через log/slog), обработку ошибок (если задача некорректна — публикуем ошибку в 'tasks.error'), graceful shutdown по сигналам SIGTERM/SIGINT."
**Результат:** Изменены файлы:
    main.go
    ┌──────────────────────────────┬─────────────────────────────────────┐
    │ Было                         │ Стало                               │
    ├──────────────────────────────┼─────────────────────────────────────┤
    │ "log"                        │ "log/slog"                          │
    ├──────────────────────────────┼─────────────────────────────────────┤
    │ 2 константы                  │ 3 константы (+ errorSubject =       │
    │ (subscribe/publish)          │ "tasks.error")                      │
    ├──────────────────────────────┼─────────────────────────────────────┤
    │ log.Printf / log.Println     │ slog.Info / slog.Error / slog.Warn  │
    ├──────────────────────────────┼─────────────────────────────────────┤
    │ signal.Notify + ручной канал │ signal.NotifyContext + nc.Drain()   │
    ├──────────────────────────────┼─────────────────────────────────────┤
    │ publishResult принимала      │ + *slog.Logger для                  │
    │ только msg + Result          │ структурированного контекста        │
    ├──────────────────────────────┼─────────────────────────────────────┤
    │ publishError публиковала в   │ публикует всегда в tasks.error (+ в │
    │ tasks.completed              │  reply при request-reply)           │
    ├──────────────────────────────┼─────────────────────────────────────┤
    │ publishError не логировала   │ log.Error(...) с атрибутами         │
    │ уровень                      │                                     │
    └──────────────────────────────┴─────────────────────────────────────┘

    segmenter.go

    Добавлен slog.Warn при отбрасывании сегментов, не дотянувших до
    minSegmentSize (100 клиентов).
### Промпт 3
**Промпт:** "Создай Dockerfile для агента сегментации (многостадийная сборка) и docker-compose.yml для запуска 4 агентов: segmentation, campaign, analytics, optimizer. Укажи зависимости, переменные окружения (NATS_URL). Добавь возможность запуска 2-3 экземпляров каждого агента через scale."
**Результат:** Созданные файлы

      1 LR13/
      2 ├── Dockerfile                          # Многостадийная сборка (golang:1.26 → alpine:3.19)
      3 ├── docker-compose.yml                  # NATS + 4 агента
      4 ├── cmd/agents/
      5 │   ├── segment/                        # сущ. — сегментация аудитории
      6 │   │   ├── go.mod / go.sum
      7 │   │   ├── main.go
      8 │   │   ├── segmenter.go
      9 │   │   └── types.go
     10 │   ├── campaign/                       # новый — рассылки
     11 │   │   ├── go.mod
     12 │   │   └── main.go
     13 │   ├── analytics/                      # новый — анализ отклика
     14 │   │   ├── go.mod
     15 │   │   └── main.go
     16 │   └── optimizer/                      # новый — оптимизация кампаний
     17 │       ├── go.mod
     18 │       └── main.go
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

## Задание 2:  Цепочки задач (pipeline).
### Промпт 1
**Промпт:** "Реализуй в оркестраторе на Python (FastAPI) последовательный pipeline: сегментация -> создание рассылки -> аналитика. Оркестратор отправляет первую задачу, ждёт результат, затем извлекает из результата нужные поля (например, список сегментов) и формирует следующую задачу. Используй nats-py и asyncio."
**Результат:** Все импорты проходят, роуты корректны.
    Созданны файлы

     1 orchestrator/
     2 ├── main.py              # FastAPI-приложение (lifespan, /health, POST 
       /pipeline)
     3 ├── pipeline.py          # Логика pipeline (send_and_wait, 
       run_pipeline)
     4 ├── models.py            # Pydantic-модели (Client, PipelineRequest, 
       PipelineResponse)
     5 ├── requirements.txt     # fastapi, uvicorn, nats-py, pydantic
     6 ├── Dockerfile           # python:3.12-slim
     7 └── .venv/               # (виртуальное окружение, создано при 
       проверке)
### Промпт 2
**Промпт:** "Добавь условный переход в pipeline: если результат аналитики (ROI) меньше 0.1, запусти дополнительный агент оптимизации. После оптимизации повтори аналитику (один раз). Реализуй сохранение статуса pipeline в Redis."
**Результат:** Все импорты проходят, роуты корректны.
### Промпт 3
**Промпт:** "Напиши интеграционный тест для pipeline с использованием pytest и моков NATS (например, nats_test). Тест должен эмулировать ответы агентов и проверять последовательность вызовов и условный переход."
**Результат:** 9/9 тестов проходят
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

## Задание  3: Распределённая трассировка (Jaeger).
### Промпт 1
**Промпт:** "Интегрируй OpenTelemetry в Go-агента: установи пакеты go.opentelemetry.io/otel/exporters/jaeger, создай глобальный tracer provider, при обработке задачи стартуй span с атрибутами (task_id, task_type). Добавь передачу контекста трассировки через NATS (в заголовках сообщения)."
**Результат:** go build + go vet проходят.
### Промпт 2
**Промпт:** "Настрой оркестратор Python для отправки трассировок в Jaeger. Используй opentelemetry-instrumentation-fastapi, добавь middleware, которая создаёт root span для каждого HTTP-запроса. Также оберни вызовы NATS в spans."
**Результат:** 
### Промпт 3
**Промпт:** "Создай docker-compose сервис для Jaeger (all-in-one) и OpenTelemetry Collector. Настрой приёма трассировок от агентов (HTTP) и оркестратора (gRPC). Добавь переменные окружения для всех сервисов (OTEL_EXPORTER_OTLP_ENDPOINT)."
**Результат:** Итоговая архитектура трассировки

      1                          ┌──────────────────┐
      2 Go Agent ──HTTP Thrift──►│                  │
      3   segment                │  otel-collector  │──OTLP gRPC──► Jaeger
      4   campaign  ──HTTP──►    │  (contrib)       │              all-in-one
      5   analytics ──HTTP──►    │  :14268 / :4317  │              :16686 (UI)
      6   optimizer ──HTTP──►    │                  │
      7                          └──────────────────┘
      8                                ▲
      9 Python Orchestrator ──OTLP────┘
     10   (gRPC :4317)

### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~20 мин

## Задание 4: Агент с состоянием (Redis).
### Промпт 1
**Промпт:** "Реализуй в агенте сегментации (Go) кэширование результатов в Redis. Перед вычислением сегментов агент проверяет наличие ключа 'segment:hash(criteria)'. Если есть — возвращает из кэша, если нет — вычисляет и сохраняет с TTL 1 час. Используй github.com/redis/go-redis/v9."
**Результат:** go build + go vet проходят.
### Промпт 2
**Промпт:** "Добавь восстановление состояния при запуске агента: агент должен загрузить из Redis все активные сегменты (по ключам 'segment:*') и хранить их в локальной мапе для быстрого доступа. При перезапуске контейнера данные не теряются."
**Результат:** Реализовано двухуровневое кэширование:

    Архитектура кэша

      1 Запрос сегментации
      2       │
      3       ▼
      4 ┌─────────────┐  HIT ──► вернуть
      5 │  localCache │        (0 наносек, без сети)
      6 │  (sync.Map) │
      7 └──────┬──────┘
      8        │ MISS
      9        ▼
     10 ┌─────────────┐  HIT ──► сохранить в localCache ──► вернуть
     11 │    Redis    │        (1 раунд по сети)
     12 │ (TTL 1 час) │
     13 └──────┬──────┘
     14        │ MISS
     15        ▼
     16 ┌─────────────┐  вычислить ──► сохранить в Redis + localCache ──► вернуть
     17 │  segment()  │
     18 └─────────────┘

### Промпт 3
**Промпт:** "Настрой Redis в docker-compose (образ redis:7-alpine) с сохранением данных (volume). Добавь в агента переменные окружения REDIS_ADDR. Проверь, что после перезапуска контейнера агента кэш восстанавливается."
**Результат:** 3/3 тестов проходят, go build + go vet OK.
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~30 мин

## Задание 5: Динамическое масштабирование.
### Промпт 1
**Промпт:** "Напиши на Python скрипт (scaler.py), который каждые 10 секунд проверяет длину очереди 'tasks.process' через NATS (nats-py, статистика JetStream или обычные подписки с количеством ожидающих сообщений). Если длина > 100 и количество текущих контейнеров агента меньше 5 — запускает дополнительный экземпляр через Docker SDK (docker.from_env().services.get('agent-segmentation').scale(реплики+1))."
**Результат:** Синтаксис OK, импорты проходят.
### Промпт 2
**Промпт:** "Интегрируй scaler.py в оркестратор как отдельный микросервис (запускается вместе с остальными в docker-compose). Добавь ему логирование и защиту от одновременного масштабирования (блокировка через Redis)."
**Результат:** Синтаксис OK, все импорты проходят.
### Промпт 3
**Промпт:** "Добавь в scaler.py возможность горизонтального масштабирования на основе метрик Prometheus (CPU, memory). Запросы к Prometheus (например, `container_cpu_usage_seconds_total`) выполняй каждые 15 секунд. Если средний CPU любого агента > 0.7 в течение 1 минуты — увеличивай реплики. Также добавь интеграцию с Kubernetes (через `kubectl scale`) при наличии KUBECONFIG, иначе используй Docker SDK."
**Результат:** Синтаксис + импорты OK, docker compose config валиден.
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

## Задание 6: Аукционное распределение задач.
### Промпт 1
**Промпт:** "Реализуй модель аукциона: агенты оптимизации (optimizer) при запуске подписываются на очередь 'auction.tasks'. При получении задачи они вычисляют свою ставку (cost = base + complexity) и публикуют её в 'auction.bids' с task_id. Оркестратор собирает ставки в течение 1 секунды, выбирает агента с минимальной стоимостью и отправляет задачу только ему (в 'tasks.process' с указанием agent_id)."
**Результат:** Go build + vet OK, 9/9 Python тестов проходят.
### Промпт 2
**Промпт:** "Добавь в агента возможность отказа от задачи, если его нагрузка велика (проверка через счётчик активных задач > 5). При отказе агент публикует bid с cost = INF. Оркестратор игнорирует такие ставки."
**Результат:** go build + go vet OK, 9/9 Python тестов проходят.
### Промпт 3
**Промпт:** "Напиши тест для аукциона (pytest), имитирующий трёх агентов с разными cost. Проверь, что побеждает агент с наименьшей стоимостью и что задача отправляется именно ему."
**Результат:** 15/15 тестов проходят (9 старых + 6 новых аукционных).
    Создан новый файл tests/test_auctioneer.py
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

## Задание 7: Интеграция LLM-агента.
### Промпт 1
**Промпт:** "Создай агента на Python (сервис llm_generator) для генерации текста рассылки. Агент подписывается на NATS (тема 'tasks.process'), принимает задачу типа 'generate_copy' с параметрами (тема, тональность). Использует Ollama (локально) с моделью llama3. Возвращает сгенерированный текст. Для связи с NATS используй nats-py."
**Результат:** Синтаксис OK, 15/15 тестов проходят, docker-compose config валиден.
### Промпт 2
**Промпт:** "Добавь fallback: если Ollama не отвечает за 15 секунд или возвращает ошибку, агент должен использовать шаблонный текст из конфига (например, 'Уникальное предложение для вас!'). Также реализуй кэширование сгенерированных текстов в Redis (по хэшу входящего промпта)."
**Результат:** Синтаксис OK, 15/15 тестов проходят.
### Промпт 3
**Промпт:** "Интегрируй LLM-агента в pipeline вместо статического шаблона. При создании рассылки оркестратор отправляет задачу 'generate_copy' в очередь, получает текст и использует его как тело сообщения. Добавь возможность переключения между LLM и шаблоном через переменную окружения."
**Результат:** 15/15 тестов проходят.
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~20 мин

## Задание 8: Веб-интерфейс для мониторинга агентов.
### Промпт 1
**Промпт:** "Создай дашборд на Streamlit (dashboard.py). Он должен подключаться к NATS и Redis. Показывать:
- Список активных агентов (из Redis или через NATS-опрос)
- Длину очереди 'tasks.process' и 'tasks.completed'
- Последние 10 выполненных задач (из лога или Redis)
Обновление раз в 5 секунд."
**Результат:** Синтаксис OK, 15/15 тестов проходят, docker-compose config валиден.
### Промпт 2
**Промпт:** ""
**Результат:** 
### Промпт 3
**Промпт:** ""
**Результат:** 
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин
